<!doctype html>
<html lang="en">
<head>
  <title>Example Application</title>
  <meta charset="UTF-8" />
  <style>
    * {
      font-family: "HK Grotesk", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, "Fira Sans", "Droid Sans", "Helvetica Neue", sans-serif;
    }
    html {
      padding: 1rem;
    }
    
    fieldset {
      word-break: break-all; font-size: 0.75rem; margin-bottom: 1rem; border-radius: 0.25rem
    }
    
    legend {
      font-weight: bold;
    }
  </style>
  <div>
    <a href="/">Home</a>
    <a href="/account">Account</a>
  
    <% if (locals.user) { %>
    <a href="/logout">Logout</a>
    <% } else { %>
    <a href="/login">Login</a>
    <% } %>

  </div>
</head>
<body>

